<div class="blog-post">
  <h2 class="blog-post-title"><%= link_to post.title, post_path(post) %></h2>
  <p class="blog-post-meta"><%= post.created_at_fmt %></p>
  <div class="tweet share-link clearfix">
    <!-- TODO: set domain -->
    <a href="http://b.hatena.ne.jp/entry/www.kyamada.com/posts/<%= post.sub_id %>"
       class="hatena-bookmark-button"
       data-hatena-bookmark-title="<%= post.title %>"
       data-hatena-bookmark-layout="standard-balloon"
       data-hatena-bookmark-lang="en"
       title="このエントリーをはてなブックマークに追加">
      <img src="http://b.st-hatena.com/images/entry-button/button-only@2x.png" alt="このエントリーをはてなブックマークに追加" width="20" height="20" style="border: none;" /></a>

    <a href="https://twitter.com/share"
      class="twitter-share-button"
      data-url="http://www.kyamada.com/posts/<%= post.sub_id %>"
      data-text="<%= post.title %>"
      data-via="">Tweet</a> <!--TODO: set twitter user id to data-via -->
  </div>

  <div class="tags">
    <% post.tags_array.each do |tag| %>
      <a href='/posts/tag/<%= tag %>'><i class="fa fa-tag"></i> <%= tag %></a>
    <% end %>
  </div>

  <% if current_user %>
    <div style="text-align:right">
      <%= link_to 'Edit', edit_post_path(post) %>
      <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  <% end %>

  <div class="body">
    <% if @keywords %>
      <%= post.body_html_by_keywords(@keywords).html_safe %>
      <%= link_to 'Read the rest', post_path(post) %>
    <% else %>
      <%= post.body_html.html_safe %>
    <% end %>
  </div>
</div><!-- /.blog-post -->
